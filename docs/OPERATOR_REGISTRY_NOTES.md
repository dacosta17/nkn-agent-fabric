# Operator Registry implementation notes

The registry is intentionally an application-layer identity primitive. It binds signed operator records to NKN addresses but does not claim permissionless Sybil resistance.

Production follow-ups are signed snapshots, revocation, key rotation, external operator attestations, and economic bonding/dispute mechanisms.
