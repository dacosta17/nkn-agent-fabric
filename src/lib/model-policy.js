export const MODEL_POLICY = Object.freeze({
  planner: 'reasoning',
  reviewer: 'reasoning',
  summarizer: 'fast',
  rule: 'Models are advisory only; deterministic validators own trust decisions.',
});
